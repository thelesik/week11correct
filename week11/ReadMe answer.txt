The recurtion sorting function got error on the Stack Overflow because the maximum stack size is exceeded (which is 1 MB by default on Windows).

C++ program overflow stack and execution was terminated. 